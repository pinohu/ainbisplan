# Controller Input

`controller_snapshot.generated.json` is written by the Lovable portfolio coordinator through the connected GitHub app. It contains the authoritative source inventory, a SHA-256 for every blueprint, build-manifest state, and the actual launchpad catalog slugs.

Do not edit the generated snapshot manually. A change to it triggers the self-contained planning and controller workflow.
